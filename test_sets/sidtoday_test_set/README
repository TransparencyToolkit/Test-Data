This test set is the SIDToday documents from 2003. It uses the same data spec
as the Snowden Document Search, but does not have anything automatically
extracted in the processed data.

Raw Document Size: 96 mb
File Types: pdf
